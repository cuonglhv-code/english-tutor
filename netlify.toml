[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"

# Increase serverless function timeout to 26 s (free tier max)
# Needed because the Anthropic AI scoring can take up to 15 s
[functions]
  timeout = 26
